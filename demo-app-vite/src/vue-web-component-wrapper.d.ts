declare module 'vue-web-component-wrapper' {
    export function createWebComponent(config: any): any; // Replace `any` with more specific types as needed
    // Define other exported functions or types as needed
}